# Email Configuration for Partnership Form
# Copy this file to .env and fill in your email settings

# Email service configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Email to receive partnership requests (defaults to EMAIL_USER if not set)
RECIPIENT_EMAIL=your-email@gmail.com

# Instructions:
# 1. For Gmail:
#    - Use your Gmail address for EMAIL_USER
#    - Generate an App Password (not your regular password) for EMAIL_PASS
#    - Go to Google Account settings > Security > 2-Step Verification > App passwords
#    - Generate a new app password and use that for EMAIL_PASS
#
# 2. For other email providers:
#    - Update EMAIL_HOST and EMAIL_PORT according to your provider's SMTP settings
#    - Use your email credentials for EMAIL_USER and EMAIL_PASS
#
# 3. Security:
#    - Never commit the .env file to your repository
#    - The .env file should be in your .gitignore